{
  "name": "fineprint",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fine Print — a reading comprehension game. Vanilla HTML/CSS/JS, no bundler, no runtime dependencies.",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "validate": "python3 scripts/validate_lesson.py public/lesson.open-window.json",
    "scan": "python3 scripts/scan_sources.py",
    "check": "npm test && npm run validate && npm run scan",
    "serve": "python3 -m http.server 8080"
  }
}
